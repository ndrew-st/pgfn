// const moduleName = 'auth'

const authRequest = 'AUTH_REQUEST'
const authSuccess = 'AUTH_SUCCESS'
const authError = 'AUTH_ERROR'

const login = 'SIGN_IN'
const register = 'SIGN_UP'
const logout = 'SIGN_OUT'

const refreshToken = 'REFRESH_TOKEN'

const initAuth = 'INIT_AUTH'

export {
  authSuccess,
  authRequest,
  authError,
  initAuth,
  refreshToken,
  login,
  logout,
  register
}
